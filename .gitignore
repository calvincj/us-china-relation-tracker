# secrets
.env

# everything the pipeline generates at runtime: the dedup database, the
# output doc, cost/review logs, the X account cache — see output/ in README.md
output/

# logs from run_scheduled.sh / run_week.sh runs
logs/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# macOS
.DS_Store

# Word lock/temp files (created automatically whenever a .docx is open)
~$*.docx

# Claude Code
.claudecode/
claude_cache/
claudecode.json
claude.log
.claude/
CLAUDE.md

# editor
.vscode/
*.swp
